import java.nio.file.Path;
import java.util.HashMap;
import java.util.List;

public class CompilationEngine {
    public CompilationEngine(List<Token> tokens, Path outputFile) {
    }

    public void compile() {
    }
}
